apiVersion: v1
kind: Service
metadata:
  name: kubernetes-service-external
  namespace: argocd
spec:
  type: ExternalName
  # This points to the kubernetes-dashboard service in the kube-syste namespace
  externalName: kubernetes-dashboard.kube-system.svc.cluster.local
  ports:
  - port: 10443
